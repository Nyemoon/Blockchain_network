LAB_DESCRIPTION="Bitcoin P2P regtest — análise de protocolo de transporte"
LAB_VERSION=1.0
LAB_AUTHOR="bitcoin-kathara-lab"

# ── Topologia ─────────────────────────────────────────────────────────────────
# LAN A: 10.0.0.0/24
# server  10.0.0.1   (nó completo, gera blocos)
# client  10.0.0.2   (nó que conecta, faz peer handshake)
#
#   [server] ───── eth0:A ───── [client]
#
# Porta P2P Bitcoin regtest: 18444/TCP

server[0]=A
server[image]=btc-node

client[0]=A
client[image]=btc-node
